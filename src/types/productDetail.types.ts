export interface ProductItemDetailInfo {
  image: string;
  shortDescription?: string;
  rate: string;
  category: string;
  name: string;
  quantity: Int16Array;
  availability: string;
  condition: string;
  brand: string;
}
